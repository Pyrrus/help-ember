
import { timewait } from 'queue/helpers/timewait';
import { module, test } from 'qunit';

module('Unit | Helper | timewait');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timewait([42]);
  assert.ok(result);
});

